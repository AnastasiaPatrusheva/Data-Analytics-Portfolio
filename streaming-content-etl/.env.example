# Скопируйте этот файл в .env и задайте свои значения.
# .env не коммитится (см. .gitignore) — секретов в репозитории нет.
#
#   cp .env.example .env

# --- ClickHouse ---
CH_USER=etl
CH_PASSWORD=change_me

# --- Superset ---
SUPERSET_SECRET_KEY=change_me_to_a_long_random_string
SUPERSET_ADMIN_USER=admin
SUPERSET_ADMIN_PASSWORD=change_me
