# --- Данные фонда: НИКОГДА в репозиторий ---
# сырая выгрузка CloudPayments
encoded_*.csv
*.csv
# витрины уровня подписки/донатора — те же данные, только с расчётными полями
powerbi_data/подписки.csv
powerbi_data/донаторы.csv
# .pbix содержит внутри полную модель данных (~3 МБ строк), выкладывается только .pbip
*.pbix
# кэш модели Power BI — тоже данные
**/.pbi/
*.abf

# --- Python ---
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# --- Служебное ---
*.log
